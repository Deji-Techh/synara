/**
 * Global concerns M25 — RTL/localization text expansion + mirror, team permissions + audit trails, legal license review.
 */
export const globalChecks = ["rtl", "localization", "team-permissions", "audit-trails", "license"] as const;
