// FILE: src/ipc/services/collaboration_service.ts
// Purpose: Stub — multi-device collaboration is stripped (Flutter-only scope).

export function broadcastCollaborationFileSnapshot(..._args: unknown[]): Promise<void> {
  return Promise.resolve();
}