// FILE: src/neon_admin/neon_prompt_context.ts
// Purpose: Stub — Neon prompt context is stripped (Flutter-only scope).

export async function buildNeonPromptForApp(..._args: unknown[]): Promise<string> {
  return "";
}